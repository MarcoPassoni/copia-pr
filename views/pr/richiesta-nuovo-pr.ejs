<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Richiesta Nuovo PR - ICONIC</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" type="image/x-icon" sizes="32x32" href="/img/favicon.ico">
    <style>
        .form-container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
        }
        
        .header-info {
            background: linear-gradient(135deg, #2196F3, #1976D2);
            color: white;
            padding: 1.5rem;
            border-radius: 8px;
            margin-bottom: 2rem;
        }
        
        .header-info h2 {
            margin: 0 0 0.5rem;
        }
        
        .header-info p {
            margin: 0;
            opacity: 0.9;
        }
        
        .form-group {
            margin-bottom: 1.5rem;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 600;
            color: #374151;
        }
        
        .form-group input, .form-group select, .form-group textarea {
            width: 100%;
            padding: 0.8rem;
            border: 1px solid #d1d5db;
            border-radius: 6px;
            font-size: 1rem;
            box-sizing: border-box;
        }
        
        .form-group input:focus, .form-group select:focus, .form-group textarea:focus {
            outline: none;
            border-color: #2196F3;
            box-shadow: 0 0 0 3px rgba(33, 150, 243, 0.1);
        }
        
        .form-row {
            display: flex;
            gap: 1rem;
        }
        
        .form-row .form-group {
            flex: 1;
        }
        
        .percentage-info {
            background: #fff3cd;
            border: 1px solid #ffc107;
            border-radius: 4px;
            padding: 0.8rem;
            margin-top: 0.5rem;
            font-size: 0.9rem;
            color: #856404;
        }
        
        .submit-btn {
            background: #10b981;
            color: white;
            border: none;
            padding: 1rem 2rem;
            border-radius: 6px;
            cursor: pointer;
            font-size: 1rem;
            font-weight: 600;
            width: 100%;
            margin-top: 1rem;
        }
        
        .submit-btn:hover {
            background: #059669;
        }
        
        .submit-btn:disabled {
            background: #9ca3af;
            cursor: not-allowed;
        }
        
        .error {
            color: #dc2626;
            font-size: 0.9rem;
            margin-top: 0.25rem;
        }
        
        .success {
            color: #059669;
            font-size: 0.9rem;
            margin-top: 0.25rem;
        }
        
        .restrictions {
            background: #f3f4f6;
            border-left: 4px solid #6b7280;
            padding: 1rem;
            margin-bottom: 1.5rem;
        }
        
        .restrictions h3 {
            margin: 0 0 0.5rem;
            color: #374151;
        }
        
        .restrictions ul {
            margin: 0;
            padding-left: 1.5rem;
        }
        
        .restrictions li {
            margin-bottom: 0.5rem;
        }
        
        /* Responsive Design */
        @media (max-width: 768px) {
            .form-container {
                margin: 10px;
                padding: 1.5rem 1rem 2rem 1rem; /* Più padding-bottom */
                margin-bottom: 2rem; /* Spazio extra in fondo */
            }
            
            .header-info {
                padding: 1rem;
                margin-bottom: 1.5rem;
            }
            
            .header-info h2 {
                font-size: 1.3rem;
            }
            
            .form-row {
                flex-direction: column;
            }
            
            .form-group {
                margin-bottom: 1rem;
            }
            
            .form-group input,
            .form-group textarea,
            .form-group select {
                font-size: 16px; /* Previene zoom su iOS */
            }
            
            .submit-btn {
                margin-bottom: 2rem; /* Spazio extra per il pulsante */
            }
        }
        
        @media (max-width: 480px) {
            .form-container {
                margin: 5px;
                padding: 1rem 0.8rem 2.5rem 0.8rem; /* Ancora più padding-bottom */
                margin-bottom: 3rem;
            }
            
            .header-info {
                padding: 0.8rem;
            }
            
            .header-info h2 {
                font-size: 1.2rem;
            }
            
            .header-info p {
                font-size: 0.9rem;
            }
            
            .form-group label {
                font-size: 0.9rem;
            }
            
            .submit-btn {
                padding: 0.8rem 1.5rem;
                font-size: 1rem;
                margin-bottom: 3rem; /* Ancora più spazio */
            }
            
            /* Assicura che il viewport sia sufficientemente alto */
            body {
                min-height: 100vh;
                padding-bottom: 2rem;
            }
        }
    </style>
</head>
<body>
    <%- include('sidebar') %>
    
    <div class="main-content">
        <div class="form-container">
            <div class="header-info">
                <h2>🚀 Richiesta Creazione Nuovo PR</h2>
                <p>Compila il modulo per richiedere la creazione di un nuovo PR. La richiesta sarà sottoposta all'approvazione dell'amministratore.</p>
            </div>
            
            <div class="restrictions">
                <h3>📋 Vincoli e Restrizioni</h3>
                <ul>
                    <li><strong>Percentuale massima:</strong> <%= userInfo.percentuale_provvigione %>% (la tua percentuale attuale)</li>
                    <li><strong>Padre selezionabile:</strong> Solo tu stesso o i tuoi PR subordinati</li>
                    <li><strong>Approvazione:</strong> La richiesta deve essere approvata da un amministratore</li>
                    <li><strong>Autorizzazione:</strong> Hai i poteri necessari per effettuare questa richiesta</li>
                </ul>
            </div>
            
            <% if (typeof message !== 'undefined') { %>
                <div class="<%= message.type === 'error' ? 'error' : 'success' %>" style="padding: 1rem; border-radius: 6px; margin-bottom: 1rem; background: <%= message.type === 'error' ? '#fee2e2' : '#d1fae5' %>;">
                    <%= message.text %>
                </div>
            <% } %>
            
            <form id="richiestaForm" method="POST" action="/pr/richiesta-nuovo-pr">
                <div class="form-row">
                    <div class="form-group">
                        <label for="nickname">Nickname *</label>
                        <input type="text" id="nickname" name="nickname" required minlength="3" maxlength="20">
                        <div class="error" id="nickname-error"></div>
                    </div>
                    <div class="form-group">
                        <label for="numero_telefono">Numero di Telefono *</label>
                        <input type="tel" id="numero_telefono" name="numero_telefono" required pattern="[0-9]{8,15}">
                        <div class="error" id="telefono-error"></div>
                    </div>
                </div>
                
                <div class="form-row">
                    <div class="form-group">
                        <label for="nome">Nome *</label>
                        <input type="text" id="nome" name="nome" required pattern="[a-zA-ZàèéìòùÀÈÉÌÒÙ'\s-]+" minlength="2">
                        <div class="error" id="nome-error"></div>
                    </div>
                    <div class="form-group">
                        <label for="cognome">Cognome *</label>
                        <input type="text" id="cognome" name="cognome" required pattern="[a-zA-ZàèéìòùÀÈÉÌÒÙ'\s-]+" minlength="2">
                        <div class="error" id="cognome-error"></div>
                    </div>
                </div>
                
                <div class="form-group">
                    <label for="password">Password *</label>
                    <input type="password" id="password" name="password" required minlength="6">
                    <div class="error" id="password-error"></div>
                </div>
                
                <div class="form-row">
                    <div class="form-group">
                        <label for="percentuale_provvigione">Percentuale Provvigione (%) *</label>
                        <input type="number" id="percentuale_provvigione" name="percentuale_provvigione" 
                               required min="0" max="<%= userInfo.percentuale_provvigione %>" step="0.01">
                        <div class="percentage-info">
                            <strong>Massimo consentito:</strong> <%= userInfo.percentuale_provvigione %>%
                        </div>
                        <div class="error" id="percentuale-error"></div>
                    </div>
                    <div class="form-group">
                        <label for="padre_proposto_id">Padre del Nuovo PR *</label>
                        <select id="padre_proposto_id" name="padre_proposto_id" required>
                            <option value="">Seleziona padre...</option>
                            <option value="<%= userInfo.id %>"><%= userInfo.nickname %> (Tu stesso)</option>
                            <% if (subordinati && subordinati.length > 0) { %>
                                <% subordinati.forEach(sub => { %>
                                    <option value="<%= sub.id %>"><%= sub.nickname %> (Tuo subordinato)</option>
                                <% }) %>
                            <% } %>
                        </select>
                        <div class="error" id="padre-error"></div>
                    </div>
                </div>
                
                <div class="form-group">
                    <label for="note_richiesta">Note della Richiesta (opzionale)</label>
                    <textarea id="note_richiesta" name="note_richiesta" rows="3" 
                              placeholder="Aggiungi eventuali note o motivazioni per questa richiesta..."></textarea>
                </div>
                
                <button type="submit" class="submit-btn" id="submitBtn">
                    📤 Invia Richiesta
                </button>
            </form>
        </div>
    </div>
    
    <script>
        const form = document.getElementById('richiestaForm');
        const submitBtn = document.getElementById('submitBtn');
        
        // Validazione in tempo reale
        form.addEventListener('input', function(e) {
            validateField(e.target);
        });
        
        function validateField(field) {
            const errorDiv = document.getElementById(field.id + '-error');
            let isValid = true;
            let errorMessage = '';
            
            switch(field.id) {
                case 'nickname':
                    if (field.value.length < 3) {
                        errorMessage = 'Il nickname deve contenere almeno 3 caratteri';
                        isValid = false;
                    } else if (!/^[a-zA-Z0-9_]+$/.test(field.value)) {
                        errorMessage = 'Il nickname può contenere solo lettere, numeri e underscore';
                        isValid = false;
                    }
                    break;
                    
                case 'numero_telefono':
                    if (!/^[0-9]{8,15}$/.test(field.value)) {
                        errorMessage = 'Inserisci un numero di telefono valido (8-15 cifre)';
                        isValid = false;
                    }
                    break;
                    
                case 'nome':
                case 'cognome':
                    if (field.value.length < 2) {
                        errorMessage = 'Deve contenere almeno 2 caratteri';
                        isValid = false;
                    } else if (!/^[a-zA-ZàèéìòùÀÈÉÌÒÙ'\s-]+$/.test(field.value)) {
                        errorMessage = 'Sono consentite solo lettere, spazi, apostrofi e trattini';
                        isValid = false;
                    }
                    break;
                    
                case 'password':
                    if (field.value.length < 6) {
                        errorMessage = 'La password deve contenere almeno 6 caratteri';
                        isValid = false;
                    }
                    break;
                    
                case 'percentuale_provvigione':
                    const maxPercentuale = <%= userInfo.percentuale_provvigione %>;
                    const valore = parseFloat(field.value);
                    if (isNaN(valore) || valore < 0) {
                        errorMessage = 'Inserisci un valore valido maggiore o uguale a 0';
                        isValid = false;
                    } else if (valore > maxPercentuale) {
                        errorMessage = `La percentuale non può superare ${maxPercentuale}%`;
                        isValid = false;
                    }
                    break;
            }
            
            if (errorDiv) {
                errorDiv.textContent = errorMessage;
                field.style.borderColor = isValid ? '#d1d5db' : '#dc2626';
            }
            
            return isValid;
        }
        
        // Validazione al submit
        form.addEventListener('submit', function(e) {
            let allValid = true;
            const requiredFields = ['nickname', 'numero_telefono', 'nome', 'cognome', 'password', 'percentuale_provvigione', 'padre_proposto_id'];
            
            requiredFields.forEach(fieldId => {
                const field = document.getElementById(fieldId);
                if (!validateField(field) || !field.value.trim()) {
                    allValid = false;
                }
            });
            
            if (!allValid) {
                e.preventDefault();
                alert('Correggi gli errori nel modulo prima di inviare la richiesta.');
            } else {
                submitBtn.disabled = true;
                submitBtn.textContent = '⏳ Invio in corso...';
            }
        });
        
        // Reset del bottone se c'è un errore di submit
        window.addEventListener('pageshow', function() {
            submitBtn.disabled = false;
            submitBtn.textContent = '📤 Invia Richiesta';
        });
    </script>
</body>
</html>
