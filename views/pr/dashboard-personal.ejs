<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>La Mia Dashboard - ICONIC</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" type="image/x-icon" sizes="32x32" href="/img/favicon.ico">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        .personal-dashboard {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .welcome-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 2rem;
            border-radius: 15px;
            margin-bottom: 2rem;
            text-align: center;
        }
        
        .welcome-header h1 {
            margin: 0 0 0.5rem;
            font-size: 2.5rem;
        }
        
        .welcome-header p {
            margin: 0;
            opacity: 0.9;
            font-size: 1.1rem;
        }
        
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }
        
        .stat-card {
            background: white;
            padding: 1.5rem;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            border-left: 4px solid #667eea;
            transition: transform 0.2s ease;
        }
        
        .stat-card:hover {
            transform: translateY(-2px);
        }
        
        .stat-header {
            display: flex;
            align-items: center;
            margin-bottom: 1rem;
        }
        
        .stat-icon {
            font-size: 2rem;
            margin-right: 1rem;
        }
        
        .stat-title {
            font-size: 0.9rem;
            color: #666;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .stat-value {
            font-size: 2.2rem;
            font-weight: bold;
            color: #333;
            margin: 0.5rem 0;
        }
        
        .stat-subtitle {
            font-size: 0.85rem;
            color: #888;
        }
        
        .chart-section {
            background: white;
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            margin-bottom: 2rem;
        }
        
        .chart-container {
            position: relative;
            height: 400px;
            width: 100%;
        }
        
        #andamentoChart {
            max-height: 400px !important;
        }
        
        .section-title {
            font-size: 1.5rem;
            color: #333;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
        }
        
        .section-title::before {
            content: '📊';
            margin-right: 0.5rem;
        }
        
        .recent-tables {
            background: white;
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }
        
        .table-container {
            overflow-x: auto;
        }
        
        .tables-list {
            width: 100%;
            border-collapse: collapse;
        }
        
        .tables-list th,
        .tables-list td {
            padding: 1rem;
            text-align: left;
            border-bottom: 1px solid #eee;
        }
        
        .tables-list th {
            background-color: #f8f9fa;
            font-weight: 600;
            color: #333;
        }
        
        .status-approved {
            color: #28a745;
            font-weight: bold;
        }
        
        .status-pending {
            color: #ffc107;
            font-weight: bold;
        }
        
        .status-rejected {
            color: #dc3545;
            font-weight: bold;
        }
        
        .currency {
            color: #667eea;
            font-weight: bold;
        }
        
        .positive {
            color: #28a745;
        }
        
        .neutral {
            color: #6c757d;
        }
        
        @media (max-width: 768px) {
            .personal-dashboard {
                padding: 1rem;
            }
            
            .welcome-header h1 {
                font-size: 2rem;
            }
            
            .stats-grid {
                grid-template-columns: 1fr;
            }
            
            .stat-value {
                font-size: 1.8rem;
            }
            
            .chart-container {
                height: 300px;
            }
        }
    </style>
</head>
<body>
    <%- include('sidebar') %>
    
    <div class="main-content">
        <div class="personal-dashboard">
            
            <div class="welcome-header">
                <h1>👋 Ciao <%= user.nickname %>!</h1>
                <p>Ecco il tuo andamento personale</p>
            </div>
            
            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-header">
                        <div class="stat-icon">🍽️</div>
                        <div>
                            <div class="stat-title">Tavoli Totali</div>
                            <div class="stat-value"><%= stats.prenotazioni_totali %></div>
                            <div class="stat-subtitle">Prenotazioni confermate</div>
                        </div>
                    </div>
                </div>
                
                <div class="stat-card">
                    <div class="stat-header">
                        <div class="stat-icon">👥</div>
                        <div>
                            <div class="stat-title">Persone Portate</div>
                            <div class="stat-value"><%= stats.persone_portate %></div>
                            <div class="stat-subtitle">Clienti serviti</div>
                        </div>
                    </div>
                </div>
                
                <div class="stat-card">
                    <div class="stat-header">
                        <div class="stat-icon">💰</div>
                        <div>
                            <div class="stat-title">Fatturato Totale</div>
                            <div class="stat-value currency">€<%= parseFloat(stats.fatturato_totale).toFixed(2) %></div>
                            <div class="stat-subtitle">Volume d'affari generato</div>
                        </div>
                    </div>
                </div>
                
                <div class="stat-card">
                    <div class="stat-header">
                        <div class="stat-icon">💎</div>
                        <div>
                            <div class="stat-title">Provvigioni Guadagnate</div>
                            <div class="stat-value currency">€<%= parseFloat(stats.provvigioni_guadagnate).toFixed(2) %></div>
                            <div class="stat-subtitle">Totale commissioni</div>
                        </div>
                    </div>
                </div>
                
                <div class="stat-card">
                    <div class="stat-header">
                        <div class="stat-icon">✅</div>
                        <div>
                            <div class="stat-title">Provvigioni Pagate</div>
                            <div class="stat-value currency">€<%= parseFloat(stats.provvigioni_pagate).toFixed(2) %></div>
                            <div class="stat-subtitle">Già ricevute</div>
                        </div>
                    </div>
                </div>
                
                <div class="stat-card">
                    <div class="stat-header">
                        <div class="stat-icon">⏳</div>
                        <div>
                            <div class="stat-title">Provvigioni Da Ricevere</div>
                            <div class="stat-value currency <%= stats.provvigioni_da_ricevere > 0 ? 'positive' : 'neutral' %>">
                                €<%= parseFloat(stats.provvigioni_da_ricevere).toFixed(2) %>
                            </div>
                            <div class="stat-subtitle">In sospeso</div>
                        </div>
                    </div>
                </div>
            </div>
            
            <div class="chart-section">
                <h2 class="section-title">Andamento Mensile</h2>
                <div class="chart-container">
                    <canvas id="andamentoChart"></canvas>
                </div>
            </div>
            
            <div class="recent-tables">
                <h2 class="section-title" style="border-bottom: none;">🕐 Ultimi Tavoli</h2>
                <div class="table-container">
                    <% if (stats.ultimi_tavoli && stats.ultimi_tavoli.length > 0) { %>
                        <table class="tables-list">
                            <thead>
                                <tr>
                                    <th>Data</th>
                                    <th>Persone</th>
                                    <th>Fatturato</th>
                                    <th>Provvigione</th>
                                    <th>Stato</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% stats.ultimi_tavoli.forEach(tavolo => { %>
                                <tr>
                                    <td><%= new Date(tavolo.data).toLocaleDateString('it-IT') %></td>
                                    <td><%= tavolo.numero_persone %></td>
                                    <td class="currency">€<%= parseFloat(tavolo.totale).toFixed(2) %></td>
                                    <td class="currency">€<%= parseFloat(tavolo.provvigione_singola).toFixed(2) %></td>
                                    <td class="status-<%= tavolo.stato && tavolo.stato.toLowerCase() === 'approvato' ? 'approved' : tavolo.stato && tavolo.stato.toLowerCase() === 'rifiutato' ? 'rejected' : 'pending' %>">
                                        <%= tavolo.stato || 'In attesa' %>
                                    </td>
                                </tr>
                                <% }); %>
                            </tbody>
                        </table>
                    <% } else { %>
                        <p style="text-align: center; color: #666; padding: 2rem;">
                            📝 Nessun tavolo ancora registrato
                        </p>
                    <% } %>
                </div>
            </div>
            
        </div>
    </div>
    
    <script>
        // Grafico andamento mensile
        const ctx = document.getElementById('andamentoChart').getContext('2d');
        
        // Prepara i dati per il grafico
        const andamentoData = <%= JSON.stringify(stats.andamento_mensile) %>;
        
        // Crea array per ultimi 6 mesi se non ci sono dati
        let mesi = [];
        let fatturato = [];
        let provvigioni = [];
        
        if (andamentoData.length > 0) {
            // Usa i dati dal database
            andamentoData.reverse().forEach(item => {
                const [anno, mese] = item.mese.split('-');
                const nomiMesi = ['Gen', 'Feb', 'Mar', 'Apr', 'Mag', 'Giu', 'Lug', 'Ago', 'Set', 'Ott', 'Nov', 'Dic'];
                mesi.push(nomiMesi[parseInt(mese) - 1] + ' ' + anno.slice(-2));
                fatturato.push(parseFloat(item.fatturato) || 0);
                provvigioni.push(parseFloat(item.provvigioni) || 0);
            });
        } else {
            // Crea dati vuoti per ultimi 6 mesi
            const nomiMesi = ['Gen', 'Feb', 'Mar', 'Apr', 'Mag', 'Giu', 'Lug', 'Ago', 'Set', 'Ott', 'Nov', 'Dic'];
            for (let i = 5; i >= 0; i--) {
                const d = new Date();
                d.setMonth(d.getMonth() - i);
                mesi.push(nomiMesi[d.getMonth()] + ' ' + d.getFullYear().toString().slice(-2));
                fatturato.push(0);
                provvigioni.push(0);
            }
        }
        
        new Chart(ctx, {
            type: 'line',
            data: {
                labels: mesi,
                datasets: [{
                    label: 'Fatturato €',
                    data: fatturato,
                    borderColor: '#667eea',
                    backgroundColor: 'rgba(102, 126, 234, 0.1)',
                    tension: 0.4,
                    fill: true
                }, {
                    label: 'Provvigioni €',
                    data: provvigioni,
                    borderColor: '#764ba2',
                    backgroundColor: 'rgba(118, 75, 162, 0.1)',
                    tension: 0.4,
                    fill: true
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        position: 'top',
                    }
                },
                scales: {
                    y: {
                        beginAtZero: true,
                        ticks: {
                            callback: function(value) {
                                return '€' + value.toFixed(0);
                            }
                        }
                    }
                },
                layout: {
                    padding: {
                        bottom: 20
                    }
                }
            }
        });
    </script>
</body>
</html>
