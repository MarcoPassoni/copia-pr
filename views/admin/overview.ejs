<div class="overview-container">
    <div class="overview-header">
        <h1>📊 Overview Generale</h1>
        <p>Vista riepilogativa completa di tutti gli amministratori e i loro risultati</p>
    </div>

    <!-- Stats Grid -->
    <div class="stats-grid">
        <div class="stat-card highlight">
            <div class="stat-label">💰 Costo Totale Tavoli</div>
            <div class="stat-value">
                € <%= totals.totalCostoTavoli.toLocaleString('it-IT', {minimumFractionDigits: 2, maximumFractionDigits: 2}) %>
            </div>
            <div class="stat-subtitle">Di tutti i PR sotto tutti gli admin</div>
        </div>

        <div class="stat-card highlight">
            <div class="stat-label">🎁 Guadagno Complessivo</div>
            <div class="stat-value">
                € <%= totals.totalGuadagnoComplessivo.toLocaleString('it-IT', {minimumFractionDigits: 2, maximumFractionDigits: 2}) %>
            </div>
            <div class="stat-subtitle">5% di tutti i tavoli</div>
        </div>

        <div class="stat-card">
            <div class="stat-label">👥 Admin Attivi</div>
            <div class="stat-value"><%= adminBreakdown.length %></div>
            <div class="stat-subtitle">Nel sistema</div>
        </div>

        <div class="stat-card">
            <div class="stat-label">🎯 PR Totali</div>
            <div class="stat-value"><%= totals.totalPR %></div>
            <div class="stat-subtitle">Sotto tutti gli admin</div>
        </div>
    </div>

    <!-- Admin Breakdown Table -->
    <div class="admin-breakdown">
        <div class="breakdown-header">
            📋 Dettaglio per Amministratore
        </div>
        <% if (adminBreakdown.length > 0) { %>
            <table class="breakdown-table">
                <thead>
                    <tr>
                        <th>Admin</th>
                        <th>PR nella Gerarchia</th>
                        <th>Costo Totale Tavoli</th>
                        <th>Guadagno (5%)</th>
                        <th>% sul Totale</th>
                    </tr>
                </thead>
                <tbody>
                    <% adminBreakdown.forEach(admin => { %>
                        <tr>
                            <td>
                                <div class="admin-name">
                                    <i class="fas fa-user-shield"></i>
                                    <%= admin.nickname %>
                                </div>
                            </td>
                            <td>
                                <span class="pr-count">
                                    <%= admin.prCount %>
                                </span>
                            </td>
                            <td>
                                <span class="cost-value">
                                    € <%= admin.costoTavoli.toLocaleString('it-IT', {minimumFractionDigits: 2, maximumFractionDigits: 2}) %>
                                </span>
                            </td>
                            <td>
                                <span class="earnings-value">
                                    € <%= admin.guadagno.toLocaleString('it-IT', {minimumFractionDigits: 2, maximumFractionDigits: 2}) %>
                                </span>
                            </td>
                            <td>
                                <div>
                                    <%= admin.percentuale.toFixed(1) %>%
                                    <div class="percentage-bar">
                                        <div class="percentage-fill" style="width: <%= admin.percentuale %>%"></div>
                                    </div>
                                </div>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } else { %>
            <div class="no-data">
                <i class="fas fa-info-circle"></i>
                Nessun amministratore trovato
            </div>
        <% } %>
    </div>

    <!-- Additional Stats -->
    <div class="stats-grid">
        <div class="stat-card">
            <div class="stat-label">💡 Info</div>
            <div class="stat-value" style="font-size: 1.5em;">
                <%= adminBreakdown.length %> Admin
            </div>
            <div class="stat-subtitle">
                gestiscono <%= totals.totalPR %> PR
            </div>
        </div>

        <div class="stat-card">
            <div class="stat-label">💵 Valore Medio Tavolo</div>
            <div class="stat-value" style="font-size: 1.5em;">
                <% 
                const tavoliCount = adminBreakdown.reduce((sum, admin) => {
                    return sum + admin.prCount;
                }, 0);
                const avgValue = tavoliCount > 0 ? totals.totalCostoTavoli / tavoliCount : 0;
                %>
                € <%= avgValue.toLocaleString('it-IT', {minimumFractionDigits: 2, maximumFractionDigits: 2}) %>
            </div>
        </div>

        <div class="stat-card">
            <div class="stat-label">📊 Media Guadagno per Admin</div>
            <div class="stat-value" style="font-size: 1.5em;">
                € <%= (totals.totalGuadagnoComplessivo / Math.max(adminBreakdown.length, 1)).toLocaleString('it-IT', {minimumFractionDigits: 2, maximumFractionDigits: 2}) %>
            </div>
        </div>

        <div class="stat-card">
            <div class="stat-label">📈 Media PR per Admin</div>
            <div class="stat-value" style="font-size: 1.5em;">
                <%= (totals.totalPR / Math.max(adminBreakdown.length, 1)).toFixed(1) %>
            </div>
        </div>
    </div>
</div>

<style>
    .overview-container {
        max-width: 1400px;
        margin: 0 auto;
        padding: 20px;
    }

    .overview-header {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        padding: 30px;
        border-radius: 15px;
        margin-bottom: 30px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.2);
    }

    .overview-header h1 {
        margin: 0 0 10px 0;
        font-size: 2.5em;
    }

    .overview-header p {
        margin: 0;
        opacity: 0.9;
        font-size: 1.1em;
    }

    .stats-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 20px;
        margin-bottom: 30px;
    }

    .stat-card {
        background: white;
        padding: 25px;
        border-radius: 10px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        border-left: 5px solid #667eea;
        transition: transform 0.3s ease;
    }

    .stat-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 6px 20px rgba(0,0,0,0.15);
    }

    .stat-card.highlight {
        background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
        color: white;
        border-left: none;
    }

    .stat-label {
        font-size: 0.9em;
        text-transform: uppercase;
        letter-spacing: 1px;
        opacity: 0.8;
        margin-bottom: 10px;
    }

    .stat-value {
        font-size: 2.5em;
        font-weight: bold;
        margin: 10px 0;
    }

    .stat-subtitle {
        font-size: 0.85em;
        opacity: 0.7;
        margin-top: 5px;
    }

    .admin-breakdown {
        background: white;
        border-radius: 10px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        overflow: hidden;
        margin-bottom: 30px;
    }

    .breakdown-header {
        background: #667eea;
        color: white;
        padding: 20px;
        font-size: 1.3em;
        font-weight: bold;
    }

    .breakdown-table {
        width: 100%;
        border-collapse: collapse;
    }

    .breakdown-table th,
    .breakdown-table td {
        padding: 15px;
        text-align: left;
        border-bottom: 1px solid #e0e0e0;
    }

    .breakdown-table th {
        background: #f5f5f5;
        font-weight: 600;
        text-transform: uppercase;
        font-size: 0.85em;
        letter-spacing: 0.5px;
    }

    .breakdown-table tr:hover {
        background: #f9f9f9;
    }

    .admin-name {
        font-weight: 600;
        color: #333;
    }

    .pr-count {
        color: #667eea;
        font-weight: 600;
    }

    .cost-value {
        color: #228B22;
        font-weight: 600;
    }

    .earnings-value {
        color: #f5576c;
        font-weight: 700;
        font-size: 1.1em;
    }

    .percentage-bar {
        height: 8px;
        background: #e0e0e0;
        border-radius: 4px;
        overflow: hidden;
        margin-top: 5px;
    }

    .percentage-fill {
        height: 100%;
        background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);
        transition: width 0.3s ease;
    }

    .no-data {
        text-align: center;
        padding: 40px;
        color: #999;
        font-style: italic;
    }

    @media (max-width: 768px) {
        .overview-header h1 {
            font-size: 1.8em;
        }

        .stats-grid {
            grid-template-columns: 1fr;
        }

        .stat-value {
            font-size: 2em;
        }

        .breakdown-table {
            font-size: 0.9em;
        }

        .breakdown-table th,
        .breakdown-table td {
            padding: 10px;
        }
    }
</style>
